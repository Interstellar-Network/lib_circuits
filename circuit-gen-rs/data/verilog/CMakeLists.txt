# To simplify the resource management of the .v, we directly embed them into the exe

# TODO on INSTALL step, copy the various .v to eg /usr/share/interstellar
# as a TEMP workaround; later it should be split into v -> skcd and skcd -> .pgarbled